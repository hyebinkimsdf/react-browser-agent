# dtype(양자화) 조정 결과 (2026-09-04)

"Qwen 한국어 품질이 별로다"는 지적에서 시작된 개선 작업 기록.

## 원인

`examples/react-vite/src/App.tsx`에서 `dtype="q4"`로 상당히 공격적인 양자화를 쓰고 있었다. 애초에 이 값을 고른 이유는 품질이 아니라 메모리였다 — Qwen3-1.7B가 WASM/WebGPU 양쪽에서 `std::bad_alloc` OOM을 냈던 전례가 있어서([App.tsx:6-19](examples/react-vite/src/App.tsx#L6-L19)), 1.7B 대신 0.6B로 내리고 dtype도 q4까지 낮춰서 안전하게 돌아가는 쪽을 택한 결과였다. 즉 "0.6B + q4"는 품질보다 안정성(OOM 회피)을 우선한 조합이었고, 한국어 출력 품질 저하는 그 대가로 남아 있던 문제였다.

## 생각

"양자화가 품질에 영향이 없는 거 아니냐"는 질문이 나와서 먼저 그 전제부터 확인했다. 양자화는 가중치를 더 적은 비트로 반올림해 저장하는 거라 모든 행렬 연산에 근사 오차가 섞이고, 이 오차는 실제로 출력 품질을 떨어뜨린다. 특히:

- 모델이 작을수록(0.6B처럼) 오차를 흡수할 파라미터 중복성이 적고
- 학습 데이터에서 비중이 작은 능력(다국어 소형 모델의 영어 외 언어 — 여기선 한국어)일수록 양자화 노이즈에 더 취약한 경향이 있다

즉 q4가 이미 약한 0.6B의 한국어 능력을 한 번 더 깎아 먹었을 가능성이 있다고 판단했고, 모델 교체(guide.md 13번 원칙 ⑤ — 조사만으로 채택 안 함, GPT/Gemma 검토 후 기각) 전에 **같은 모델에서 가장 싸게 시도해볼 수 있는 레버**로 dtype 상향을 먼저 테스트하기로 했다. fp16은 0.6B 기준 q4보다 가중치 용량이 3~4배 커질 것으로 예상돼([App.tsx:152] 근처 참고) OOM 리스크가 없진 않았지만, 1.7B-q4만큼 큰 체급은 아니라 시도해볼 가치가 있다고 봤다.

## 시도

`BrowserAIProvider`의 `dtype` prop을 `"q4"` → `"fp16"`으로 변경. ([App.tsx:152](examples/react-vite/src/App.tsx#L152))

```diff
- dtype="q4"
+ dtype="fp16"
```

## 결과

사용자가 실제 브라우저에서 직접 테스트: OOM 없이 정상 로딩됐고, **한국어 출력 품질이 눈에 띄게 개선됐다**고 확인. 다만 이번엔 [TOOL-ROUTER-RESULTS.md](TOOL-ROUTER-RESULTS.md)의 응답 속도 측정 때와 달리 Playwright로 별도 자동 검증은 하지 않았다 — 사용자의 실사용 기준 주관적 평가이며, 정량적 비교(예: 같은 프롬프트에 대한 q4 vs fp16 출력 비교, BLEU/사람 평가 등)는 아직 없다.

## 결론 / 남은 과제

- q4 → fp16 전환으로 한국어 품질 문제는 완화된 것으로 보인다. "양자화는 품질에 영향 없다"는 전제는 틀렸다는 것도 이번에 확인된 셈이다.
- fp16은 다운로드 용량·메모리 사용량이 q4보다 커진 트레이드오프가 있다 — 저사양 기기나 느린 네트워크 환경에서도 문제없이 로딩되는지는 아직 별도로 확인하지 않았다.
- 필요하면 `q8`이 q4와 fp16 사이의 중간 지점으로 남아 있다 (용량은 fp16보다 작고 품질은 q4보다 나을 것으로 예상, 미검증).
- 정량적 품질 비교(같은 질문에 대한 q4/q8/fp16 출력 나란히 비교)는 아직 안 했다 — 다음에 필요하면 진행.

## 추가 시도: fp16 → fp32는 어떨까?

### 원인

fp16→q4 방향(양자화를 더 낮추면 빨라지지만 품질이 떨어짐)은 이미 확인했으니, 반대로 fp16보다 정밀도를 더 올리면(fp32) 속도와 품질을 동시에 잡을 수 있는지 궁금하다는 질문이 나왔다.

### 생각

정밀도 축은 한쪽으로만 움직인다 — 올릴수록 품질은 (많아야) 살짝 좋아지고 속도/메모리는 반드시 나빠진다. "더 빠르면서 더 좋아지는" 방향은 이 축 위에 없다고 미리 답했다. 게다가 fp32는 0.6B 기준 가중치만 약 2.4GB로 추정되는데, 이는 이미 OOM났던 1.7B-q4(~0.85GB 추정)보다도 훨씬 크다 — 로딩 자체가 안 될 가능성이 높다고 예상하고 시도했다.

### 시도

`dtype`을 `"fp16"` → `"fp32"`로 변경 후 headless Chromium(Playwright)으로 로딩 확인.

### 결과

**예상대로 OOM.** 62초간 로딩을 시도하다 다음 에러로 세션 생성 자체가 실패했다:

```
Can't create a session. ERROR_CODE: 6, ERROR_MESSAGE: std::bad_alloc
```

1.7B-q4 때와 동일한 `std::bad_alloc` 패턴 — 이 WASM 환경의 메모리 상한을 넘어선 것으로 보인다. `dtype`은 마지막 안전 값인 `fp16`으로 되돌렸다.

### 결론

- "정밀도를 더 올리면 더 빠르고 품질도 좋아진다"는 가설은 기각됐다 — 애초에 성립할 수 없는 트레이드오프였고, 실측으로도 OOM으로 확인됐다.
- 이 스택(0.6B, WASM, headless 검증 환경)에서 현실적인 dtype 상한은 `fp16`이다. 그 이상(`fp32`)은 시도할 필요가 없다는 게 이번에 확정됐다.
- 남은 레버는 여전히 `q8`(fp16보다 가볍고 q4보다 품질 나을 것으로 예상, 미검증)과 모델 자체 교체뿐이다.
